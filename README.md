Proyecto reconstruido a partir de red_bibliometrica2.html.

Pasos para ejecutar:

1. Crear y activar un entorno virtual Python.
2. Instalar dependencias: `pip install -r requirements.txt`.
3. Ejecutar: `python run_app.py`.

La plantilla base está en `red_bibliometrica2.html`. La app sirve `templates/index.html` y expone `/api/data` y `/api/matrix`.
